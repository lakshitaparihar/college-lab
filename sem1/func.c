#include <stdio.h>
int addfunc(a,b){
    c = a + b ;
}
int main(){
    printf("inter the numbers to be added:");
    int a, b;
    scanf("%d %d", &a &b);
    int add;
    add= addfunc(a,b);
    printf("sum is %d, add");
}
